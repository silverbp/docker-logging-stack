Splunk Logging with a Forwarder for Docker
=========================================================
---
#### Steps to run ####
To run clone repo and do `docker-compose up` in the repo directory
You can then load splunk on port 8000